apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-server.fullname" . }}-test-connection"
  labels:
    {{- include "log-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
